<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Note App</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
      <a class="text-blue-500 inline-block mb-10" href="/">Go Back</a>
      <div class="form p-10 text-white">
        <form action="/edit" method="post">
          <!-- Previous title -->
          <input class="block w-1/4 rounded-md outline-none px-5 py-2
          text-zinc-600 bg-zinc-800" type="text" value="<%=
          fileName.split(".")[0] %>" name="previousTitle" readOnly="true" />
          <!-- Previous title -->
          <input
            class="block w-1/4 rounded-md outline-none px-5 py-2 mt-2 bg-zinc-800"
            type="text"
            placeholder="New Title"
            name="newTitle"
          />
          <!-- Button -->
          <input
            class="mt-5 bg-yellow-600 px-5 py-2 rounded-md"
            type="submit"
            value="Edit Title"
          />
        </form>
      </div>
    </div>
  </body>
</html>
